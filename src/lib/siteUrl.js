export function getSiteUrl() {
	// let siteUrl = process.env.CF_PAGES_URL ?? 'https://rivet.dev';  // CF_PAGES_URL returns the pages.dev URL for prod
	const siteUrl = "https://rivet.dev";
	return siteUrl;
}
