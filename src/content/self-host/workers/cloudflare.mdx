---
title: "Cloudflare Workers"
description: "Run Rivet workers on Cloudflare Workers."
---

Cloudflare Workers is a **serverless** platform: there is no long-lived process, so the control plane calls your `/api/rivet` endpoint to start actors. The Worker URL has to be public and registered.

## Requirements

- A [Cloudflare account](https://dash.cloudflare.com/)
- [`wrangler`](https://developers.cloudflare.com/workers/wrangler/) configured for your account
- A control plane, either [Rivet Cloud](https://dashboard.rivet.dev) or <SelfHostLink to="control-plane">your own</SelfHostLink>

<ProductSlot name="requirements" />

<ProductSlot name="caveats" />

## Steps

<Steps>

<Step title="Configure wrangler">

`nodejs_compat` is required.

```toml wrangler.toml
name = "rivet-worker"
main = "src/index.ts"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]

[vars]
RIVET_ENDPOINT = "https://your-namespace:sk_...@api.rivet.dev"
RIVET_PUBLIC_ENDPOINT = "https://your-namespace@api.rivet.dev"
```

Both values are described in <SelfHostLink to="workers">Workers</SelfHostLink>. For a production deployment, set `RIVET_ENDPOINT` with `wrangler secret put` rather than committing it in `[vars]`.

<ProductSlot name="snippet" />

</Step>

<Step title="Deploy">

```sh
npx wrangler deploy
```

</Step>

<Step title="Register the URL">

Set the Worker URL with the `/api/rivet` path as the serverless runner URL in the dashboard.

</Step>

</Steps>

<ProductSlot name="env" />

<ProductSlot name="verify" />

## Scaling

Cloudflare autoscales Workers for you: there are no instances or replicas to configure. The one scaling bound you own is `maxConcurrentActors` on the serverless runner config, which caps concurrent actors across the whole pool and defaults to `1000`. See <SelfHostLink to="workers/production-checklist">the production checklist</SelfHostLink>.

## Next steps

- <SelfHostLink to="workers/production-checklist">Production checklist</SelfHostLink>
- <SelfHostLink to="workers/supabase">Supabase Functions</SelfHostLink>, the other WebAssembly-runtime target
