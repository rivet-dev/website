---
title: "Docker Compose"
description: "Run Rivet workers with Docker Compose."
---

Workers run in **runner** mode here: the container opens a connection out to the control plane on startup. It needs no published port.

## Requirements

- Docker with the Compose plugin
- A control plane, either [Rivet Cloud](https://dashboard.rivet.dev) or <SelfHostLink to="control-plane">your own</SelfHostLink>

<ProductSlot name="requirements" />

<ProductSlot name="caveats" />

## Steps

<Steps>

<Step title="Package your app">

```dockerfile
FROM node:24-slim
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY . .
CMD ["node", "server.js"]
```

`CMD` invokes the binary directly rather than `npm start`, so the process receives `SIGTERM` and can drain.

Rivet requires Node 22 or newer. Alpine images need a build toolchain because Rivet's SQLite dependency has no musl prebuilt binary, so the Debian-based image is the smaller working choice.

<ProductSlot name="snippet" />

</Step>

<Step title="Add the service">

`stop_grace_period` matters here. Actors drain for up to 30 minutes, and Compose's default of 10 seconds cuts them off.

```yaml
services:
  my-app:
    build: .
    environment:
      RIVET_ENDPOINT: "${RIVET_ENDPOINT}"
      RIVET_PUBLIC_ENDPOINT: "${RIVET_PUBLIC_ENDPOINT}"
    stop_grace_period: 35m
    restart: unless-stopped
```

Both endpoint values are described in <SelfHostLink to="workers">Workers</SelfHostLink>. Put them in a `.env` file next to the Compose file and keep it out of version control.

</Step>

<Step title="Run alongside a self-hosted control plane">

When both halves live in the same Compose file, the worker reaches the control plane by service name over the Compose network:

```yaml
  my-app:
    build: .
    environment:
      RIVET_ENDPOINT: "http://default:${RIVET_ADMIN_TOKEN}@rivet-engine:6420"
    depends_on:
      - rivet-engine
    stop_grace_period: 35m
    restart: unless-stopped
```

See <SelfHostLink to="control-plane/docker-compose">the control plane guide</SelfHostLink> for the other half of this file.

</Step>

<Step title="Create the runner config">

A worker is refused until a runner config exists for its pool. Against a self-hosted control plane, create it once:

```bash
curl -X PUT "http://localhost:6420/runner-configs/default?namespace=default" \
  -H "Authorization: Bearer $RIVET_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"datacenters":{"default":{"normal":{}}}}'
```

Rivet Cloud does this for you. See <SelfHostLink to="workers">Runner configs</SelfHostLink>.

</Step>

<Step title="Start it">

```bash
docker compose up -d
```

</Step>

<Step title="Verify">

```bash
docker compose ps
```

The service should be `running`. It then appears under **Runners** in the dashboard.

</Step>

</Steps>

<ProductSlot name="env" />

<ProductSlot name="verify" />

## Next steps

- <SelfHostLink to="workers/production-checklist">Production checklist</SelfHostLink>
- <SelfHostLink to="control-plane/docker-compose">Run the control plane with Docker Compose</SelfHostLink>
