# https://www.robotstxt.org/robotstxt.html
User-agent: *
Allow: /

# Disallow API and internal routes.
# /typedoc/ is intentionally crawlable so crawlers can see its X-Robots-Tag:
# noindex header (set in the Caddyfile); a Disallow here would hide that header.
Disallow: /api/
Disallow: /internal/

# Sitemap
Sitemap: https://rivet.dev/sitemap-index.xml
