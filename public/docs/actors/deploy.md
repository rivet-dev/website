# Deploy

Run Rivet Actors on Rivet Cloud or your own infrastructure.

**TODO.** The Deploy tab is being written from scratch. It merges the old `Deploy` and `Self-Hosting` sections into one story: pick where actors run, then follow one path.

**Source material:** `website/src/content/_pending-rewrite/deploy/` and `website/src/content/_pending-rewrite/self-hosting/`