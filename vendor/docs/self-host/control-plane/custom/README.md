# Custom Platform — the control plane

Deployment artifacts for running the control plane on Custom Platform.

These files are embedded into the docs with `<CodeSnippet>`, so the guide and
this directory can never drift. Edit them here, not in the MDX.

**Guide:** <https://rivet.dev/actors/self-host/control-plane/custom>

## Files

- `commands-2.sh`
- `commands.sh`
