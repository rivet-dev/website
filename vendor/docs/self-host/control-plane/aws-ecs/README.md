# AWS ECS — the control plane

Deployment artifacts for running the control plane on AWS ECS.

These files are embedded into the docs with `<CodeSnippet>`, so the guide and
this directory can never drift. Edit them here, not in the MDX.

**Guide:** <https://rivet.dev/actors/self-host/control-plane/aws-ecs>

> **TODO** — this platform had no existing guide to port from. Add the deployment artifacts it needs.

## Files

_No artifacts yet._
