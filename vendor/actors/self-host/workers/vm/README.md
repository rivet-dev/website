# Virtual Machine — workers

Deployment artifacts for running workers on Virtual Machine.

These files are embedded into the docs with `<CodeSnippet>`, so the guide and
this directory can never drift. Edit them here, not in the MDX.

**Guide:** <https://rivet.dev/actors/self-host/workers/vm>

## Files

- `commands-2.sh`
- `commands-3.sh`
- `commands-4.sh`
- `commands.sh`
