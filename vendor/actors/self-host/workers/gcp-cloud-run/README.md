# Google Cloud Run — workers

Deployment artifacts for running workers on Google Cloud Run.

These files are embedded into the docs with `<CodeSnippet>`, so the guide and
this directory can never drift. Edit them here, not in the MDX.

**Guide:** <https://rivet.dev/actors/self-host/workers/gcp-cloud-run>

## Files

- `Dockerfile`
- `commands-2.sh`
- `commands-3.sh`
- `commands.sh`
