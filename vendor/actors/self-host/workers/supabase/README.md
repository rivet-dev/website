# Supabase Functions — workers

Deployment artifacts for running workers on Supabase Functions.

These files are embedded into the docs with `<CodeSnippet>`, so the guide and
this directory can never drift. Edit them here, not in the MDX.

**Guide:** <https://rivet.dev/actors/self-host/workers/supabase>

## Files

- `commands-2.sh`
- `commands.sh`
