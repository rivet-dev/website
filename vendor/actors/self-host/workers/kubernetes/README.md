# Kubernetes — workers

Deployment artifacts for running workers on Kubernetes.

These files are embedded into the docs with `<CodeSnippet>`, so the guide and
this directory can never drift. Edit them here, not in the MDX.

**Guide:** <https://rivet.dev/actors/self-host/workers/kubernetes>

## Files

- `Dockerfile`
- `commands-2.sh`
- `commands-3.sh`
- `commands.sh`
- `rivetkit-app.yaml`
- `rivetkit-secrets.yaml`
