# Railway — the control plane

Deployment artifacts for running the control plane on Railway.

These files are embedded into the docs with `<CodeSnippet>`, so the guide and
this directory can never drift. Edit them here, not in the MDX.

**Guide:** <https://rivet.dev/actors/self-host/control-plane/railway>

## Files

- `commands.sh`
