# Virtual Machine — the control plane

Deployment artifacts for running the control plane on Virtual Machine.

These files are embedded into the docs with `<CodeSnippet>`, so the guide and
this directory can never drift. Edit them here, not in the MDX.

**Guide:** <https://rivet.dev/actors/self-host/control-plane/vm>

## Files

- `commands-2.sh`
- `commands.sh`
